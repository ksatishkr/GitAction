name: multiJobs action
on: 
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
env:
  myName: "satish"
  age: 22
  from: "bihar"

jobs: 
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: single line comment
        run: echo this is single line comment
      - name: ${{env.myName}}
        run: |
          echo this is for testing code,
          echo test, for testing perpose

  ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: single line comment
        run: echo this is single line comments
      - name: multiple-line runner
        run: echo this is multiple line comments

  macos: 
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: single line comment
        run: echo this is single line comment
      - name: multiple line comments
        run: echo this is 
      
